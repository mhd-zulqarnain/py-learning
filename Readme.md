- to create  code block jupyter environment ( escape+a)

- key difference 
    - to run itrations
        - enumurate() to loop with index in list and tuple
        - items()to loop with index in dictionaries
    - core difference
        - list are mutable while tuple are immutable
        - in dictionaries we use key-value pair the keys are immutable


- Kotlin mental mapping difference between *args and **args
- Python	Kotlin
- *args	    vararg
- **kwargs	Map<String, Any>
